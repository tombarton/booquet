# Migration `20200506195948-update-the-majority-of-ids-to-be-strings`

This migration has been generated by Tom Barton at 5/6/2020, 7:59:48 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "public"."Cart","public"."CartItem","public"."Order","public"."OrderItem","public"."Product","public"."User";

CREATE TYPE "Role" AS ENUM ('ADMIN', 'USER');

CREATE TYPE "OrderStatus" AS ENUM ('PENDING', 'AWAITING_CONFIRMATION', 'AWAITING_FULFILLMENT', 'FULFILLED');

CREATE TABLE "public"."User" (
    "email" text  NOT NULL ,
    "firstname" text  NOT NULL ,
    "id" text  NOT NULL ,
    "lastname" text  NOT NULL ,
    "password" text  NOT NULL ,
    "registeredAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "resetToken" text   ,
    "resetTokenExpiry" timestamp(3)   ,
    "role" "Role" NOT NULL ,
    "updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Product" (
    "id" text  NOT NULL ,
    "name" text  NOT NULL ,
    "price" integer  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."CartItem" (
    "cardId" text  NOT NULL ,
    "id" text  NOT NULL ,
    "productId" text  NOT NULL ,
    "quantity" integer  NOT NULL DEFAULT 1,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Cart" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "id" text  NOT NULL ,
    "userId" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."OrderItem" (
    "id" text  NOT NULL ,
    "image" text  NOT NULL ,
    "orderId" integer  NOT NULL ,
    "price" integer  NOT NULL ,
    "quantity" integer  NOT NULL ,
    "title" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Order" (
    "chargeId" text  NOT NULL ,
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "id" SERIAL,
    "status" "OrderStatus" NOT NULL ,
    "total" integer  NOT NULL ,
    "updatedAt" timestamp(3)  NOT NULL ,
    "userId" text   ,
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "User.resetToken" ON "public"."User"("resetToken")

CREATE UNIQUE INDEX "Cart.userId" ON "public"."Cart"("userId")

CREATE UNIQUE INDEX "Cart_userId" ON "public"."Cart"("userId")

ALTER TABLE "public"."CartItem" ADD FOREIGN KEY ("productId")REFERENCES "public"."Product"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."CartItem" ADD FOREIGN KEY ("cardId")REFERENCES "public"."Cart"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Cart" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."OrderItem" ADD FOREIGN KEY ("orderId")REFERENCES "public"."Order"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Order" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200506194124-make-user-optional-for-order..20200506195948-update-the-majority-of-ids-to-be-strings
--- datamodel.dml
+++ datamodel.dml
@@ -3,15 +3,15 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("POSTGRESQL_URL")
 }
 model User {
   email            String    @unique
   firstname        String
-  id               Int       @default(autoincrement()) @id
+  id               String    @default(uuid()) @id
   lastname         String
   password         String
   registeredAt     DateTime  @default(now())
   resetToken       String?   @unique
@@ -34,28 +34,28 @@
   CartItem CartItem[]
 }
 model CartItem {
-  id        Int     @default(autoincrement()) @id
+  id        String  @default(uuid()) @id
   product   Product @relation(fields: [productId], references: [id])
   productId String
   quantity  Int     @default(1)
   cart      Cart    @relation(fields: [cardId], references: [id])
-  cardId    Int
+  cardId    String
 }
 // Cart in the DB is only used for logged in users. This allows us to persist
 // the basket across devices/browsers/etc.
 model Cart {
-  id        Int        @default(autoincrement()) @id
+  id        String     @default(uuid()) @id
   user      User       @relation(fields: [userId], references: [id])
-  userId    Int        @unique
+  userId    String     @unique
   createdAt DateTime   @default(now())
   CartItems CartItem[]
 }
 model OrderItem {
-  id       Int    @default(autoincrement()) @id
+  id       String @default(uuid()) @id
   title    String
   image    String
   price    Int
   quantity Int
@@ -78,6 +78,6 @@
   chargeId  String
   createdAt DateTime    @default(now())
   updatedAt DateTime    @updatedAt
   user      User?       @relation(fields: [userId], references: [id])
-  userId    Int?
+  userId    String?
 }
```


