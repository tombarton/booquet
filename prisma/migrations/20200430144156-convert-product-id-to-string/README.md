# Migration `20200430144156-convert-product-id-to-string`

This migration has been generated by Tom Barton at 4/30/2020, 2:41:56 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "public"."CartItem","public"."Product","public"."User";

CREATE TYPE "Role" AS ENUM ('ADMIN', 'USER');

CREATE TABLE "public"."User" (
    "email" text  NOT NULL ,
    "firstname" text  NOT NULL ,
    "id" SERIAL,
    "lastname" text  NOT NULL ,
    "password" text  NOT NULL ,
    "registeredAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "resetToken" text   ,
    "resetTokenExpiry" timestamp(3)   ,
    "role" "Role" NOT NULL ,
    "updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Product" (
    "id" text  NOT NULL ,
    "name" text  NOT NULL ,
    "price" Decimal(65,30)  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."CartItem" (
    "id" SERIAL,
    "productId" text  NOT NULL ,
    "quantity" integer  NOT NULL DEFAULT 1,
    "userId" integer  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "User.resetToken" ON "public"."User"("resetToken")

ALTER TABLE "public"."CartItem" ADD FOREIGN KEY ("productId")REFERENCES "public"."Product"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."CartItem" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200428102104-add-product-table..20200430144156-convert-product-id-to-string
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("POSTGRESQL_URL")
 }
 model User {
   email            String     @unique
@@ -26,17 +26,17 @@
   USER
 }
 model Product {
-  id    Int    @id
+  id    String @id
   name  String
   price Float
 }
 model CartItem {
   id        Int     @default(autoincrement()) @id
   product   Product @relation(fields: [productId], references: [id])
-  productId Int
+  productId String
   quantity  Int     @default(1)
   user      User    @relation(fields: [userId], references: [id])
   userId    Int
 }
```


