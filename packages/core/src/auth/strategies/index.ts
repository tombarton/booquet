import { JwtStrategy } from './jwt.strategy';
import { OptionalStrategy } from './optional.strategy';

export { JwtStrategy, OptionalStrategy };
export const Strategies = [JwtStrategy, OptionalStrategy];
